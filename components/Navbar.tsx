"use client"

/**
 * Navbar (legada) – mantida para compatibilidade histórica.
 * A navegação principal agora é gerenciada por:
 *   - components/Sidebar.tsx   (sidebar desktop + bottom nav mobile)
 *   - components/TopBar.tsx    (barra superior com usuário / logout)
 *   - app/dashboard/layout.tsx (shell do dashboard)
 */
export function Navbar() {
  return null
}
